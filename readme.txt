PROBLEMA:

Partendo dal markup della versione svolta in js plain, rifare lo slider ma questa volta usando Vue.
In allegato trovate uno zip con già il markup base dell'html, css ed il js già impostato con l'array di oggetti.

Bonus:
1- al click su una thumb, visualizzare in grande l'immagine corrispondente
2- applicare l'autoplay allo slider: ogni 3 secondi, cambia immagine automaticamente
3- quando il mouse va in hover sullo slider, bloccare l'autoplay e farlo riprendere quando esce

1- Inserire le immagini tramite array di oggetti con Vue js
    1.1- Inserire almeno un div degli oggetti che vogliamo inserire così applicarvi il ciclo for come da Vue JS
    1.2- Inserire le classi basandoti sulla condizioni di itemActive == index
2- Implementare la funzione sulle frecce
3- Inserire i pulsanti per l'autoplay, lo stop e il reversePlay
4- Implementare lo stop se si va con il mouse sulla pagina e il proseguo di autoplay o viceversa quando si esce